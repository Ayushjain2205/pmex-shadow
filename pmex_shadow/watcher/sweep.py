"""Data API poll-per-target sweep (FR-W-6). Not implemented in Phase 0 — Phase 1 scope.

Endpoint shape (`GET /trades?user=<proxy>`, field `proxyWallet`) is already verified
in docs/VERIFIED.md item 5.
"""
